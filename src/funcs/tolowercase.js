/*
    tolowercase.js : function that lower-cases the text

    Parameter:
        text: string obj
    
    How to use:
        ToLowerCase(text) //renders everything lowercase
*/

const ToLowerCase = (text)=>text.toLowerCase();

export {ToLowerCase};
